name: 'Extract Branch Name Action'
description: 'Extracts the name of the current branch'
author: 'Bernardo Mart�nez Garrido'
outputs:
  branchName:
    description: 'The current Github branch.'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'git-branch'
  color: 'green'